#include <stdio.h>

int main(void) {
    int count, i;
    printf("What integer do you want to count to?\n");
    scanf("%d", &count);

    i = 0;
    if (i < count; i++)
        {
        printf("%d", i);
        }

    }
    return 0;
}
